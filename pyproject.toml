[project]
name = "el-carbon-intensity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "hopsworks[great-expectations,polars,python]>=4.6.0",
    "pydantic-settings>=2.12.0",
]
